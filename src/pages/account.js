import React from 'react';

const Account = () => {
  return (
    <div
      style={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        
      }}
    >
      <h1 className = "White">Account</h1>
    </div>
  );
};

export default Account;